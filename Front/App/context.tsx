import React, { Component } from "react";

import {
  Text,
  View,
  StyleSheet,
  Image,
  ScrollView,
  Dimensions,
  ActivityIndicator,
} from "react-native";

export const NetworkContext = React.createContext();
